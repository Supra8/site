App.Models = App.Models || {};

(function () {
  'use strict';
  App.Models.Metric = class Metric extends Backbone.Model {
    url() {

    }

    validate(attrs, options) {

    }

    parse(response, options) {
      return response;
    }
  };
})();
